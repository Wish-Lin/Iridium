add ellipse (orthogonal only at first)
template selector
improve ALL function plotting: 
	a better replace method, so that stuff like tan(x) can be used properly.

insert function,command and special characters wherever I need.










Functions that are finished but the description isn't completed yet:
display()
ellipse()
slope_field()
polygon_rc()
polygon_ri()
polygon_rs()
polygon_rv()

can't draw on top of drawimage()

la_line_pp()
la_rectangle()

line_ps()
n_derivative()


change all childwindows to hideable <div>s.
